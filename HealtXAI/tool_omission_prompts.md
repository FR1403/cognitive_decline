# Tool Omission Prompts

Prompt completo usato per ciascuna task rilevante di `tool omission`.

## Task 1

Action type: `1`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant retrieves broom from supply closet

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 2

Action type: `1`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant retrieves duster from supply closet

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 3

Action type: `1`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant retrieves dust pan and brush from closet

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 4

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant sweeps kitchen floor

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 5

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant uses dust pan and brush

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 6

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant dusts living room

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 7

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant dusts dining room

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 8

Action type: `1`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant retrieves materials from cupboard A

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 9

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant fills dispenser with medication

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 10

Action type: `1`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant retrieves birthday card

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 11

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant writes birthday wish inside the card: Happy Birthday Name Wish

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 12

Action type: `1`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant retrieves check

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 13

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant writes check for birthday card

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 14

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant puts birthday card and check in envelope

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 15

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant addresses the envelope

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 16

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant selects correct DVD

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 17

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participants uses remote to turn on TV

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 18

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant selects correct channel

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 19

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant turns off T.V.

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 20

Action type: `1`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant retrieves watering can from supply closet

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 21

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant fills watering can

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 22

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant waters plants (windowsill

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 23

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant waters plants (coffee table

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 24

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant empties extra water into sink

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 25

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant fills measuring cup with water

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 26

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant boils water in microwave

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 27

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant pours water into cup of noodles

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 28

Action type: `1`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant retrieves pitcher of water from refrigerator

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 29

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant pours glass of water

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 30

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant chooses correct outfit from closet:

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 31

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant lays out clothes on living room couch

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 32

Action type: `1`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant retrieves light bulbs from cabinet B

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 33

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant removes old light bulb

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 34

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant replaces the bulb

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 35

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant turns on water

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 36

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant uses hand soap

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 37

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant washes hands

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 38

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant dries hands

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 39

Action type: `10`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant locates the sponge

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 40

Action type: `10`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant locates the dish detergent

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 41

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant wets the sponge

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 42

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant lathers the sponge with the detergent

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 43

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant washes the countertops

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 44

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant rinses out the sponge

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 45

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant dries the countertops

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 46

Action type: `10`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant locates the phone book

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 47

Action type: `10`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant locates the phone number for Safeway Food and Drugs

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 48

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant dials the number and listens to the recording

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 49

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant records the recipe

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 50

Action type: `10`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant locates the laundry basket

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 51

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant sorts/folds the laundry

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 52

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant removes materials from cupboard

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 53

Action type: `10`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant locates pot

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 54

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant turns on stove

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 55

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant fills pot with water:

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 56

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant puts oatmeal into pot:

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 57

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant stirs oatmeal

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 58

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant puts oatmeal into bowl

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 59

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant turns stove off

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 60

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant puts sugar in oatmeal

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 61

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant puts raisin in oatmeal

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 62

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant gathers statements from bookshelf

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 63

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant sorts pile of statements into the organizer

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 64

Action type: `10`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant locates deck of cards from kitchen cabinet B

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 65

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant brings deck of cards to the dining room table

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 66

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant sets up card game

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```

## Task 67

Action type: `5`

```text
Identify the object or tool most likely used to perform this action.
Return exactly one object/tool name.
If multiple objects are mentioned, choose the instrument that executes the action,
not the object receiving it.
No parentheses. No explanatory notes. No alternatives.
No explanations. No JSON. No full sentence.
If no tool or object is used, return an empty string.

Action: Participant explains the rules of the game over the phone

Examples:
- sweep kitchen floor -> broom
- stir soup in a bowl -> spoon
- cut bread with a knife -> knife
- pour water into a glass -> pitcher
```
